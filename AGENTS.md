# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v57.0.0/ before writing any code.

# GitHub

This project's remote is `https://github.com/appdevstores/kinetic` — owned by the
**appdevstores** account. The user has multiple GitHub accounts for different
projects; never use the `Harris-Pothen_hibu` account here. Repo-local git
identity is set (`user.name=appdevstores`,
`user.email=325010025+appdevstores@users.noreply.github.com`), and `gh` is the
git credential helper.
